(function (window)) {
  'use strict';
  // TODO: this will create a new quiz

  // store the name on the gmail account (will likely need to do in main)
  // user uid can be found from firebsae.auth().currentuser.uid
  console.log("The userID is " + firebase.auth().currentUser.uid);

  // ask what they want to name the new quiz

  // get row values and column names
  // get question, answer

  // store their data in the json at accountName/quizName

})(window);

